name: Salesforce Deployment Pipeline

on:
  pull_request:
    branches:
      - main

jobs:
  validate_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Install Salesforce CLI         
        run: |
          npm install sfdx-cli
          node_modules/sfdx-cli/bin/run --version
          node_modules/sfdx-cli/bin/run plugins --core

      - name: Authenticate to Production Org
        run: sfdx force:auth:jwt:grant --clientid=3MVG9YFqzc_KnL.wy27qddHiZxqYjPBGrIihfnbuTNYTLyoJYG3Rw6O7sks48tuWPhIdPqcUExfrYV9U7vWJ3 --jwtkeyfile=C:/Users/dhiggi02/.ssh/jwt/server.key --username=diarmuid.higgins1992@gmail.com.privateappexchangeprojectsprod --setdefaultusername --setalias PERSONALPROJECTSAPPEXCHANGEPROD 
      - name: Validate against Production Org
        run: |
          # Run validation commands here, for example:
          sfdx force:source:deploy -p force-app -l RunLocalTests -u PERSONALPROJECTSAPPEXCHANGEPROD

      - name: Deploy to Production Org
        if: github.event_name == 'pull_request' && github.event.action == 'closed' && github.event.pull_request.merged == true
        run: |
          # Deploy to production only when PR is merged 
          sfdx force:source:deploy -p force-app -u PERSONALPROJECTSAPPEXCHANGEPROD
